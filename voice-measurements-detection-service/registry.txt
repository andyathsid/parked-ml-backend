ACCOUNT=863518452414
REGION=ap-southeast-1
REGISTRY=parked-images
PREFIX=${ACCOUNT}.dkr.ecr.${REGION}.amazonaws.com/${REGISTRY}
TAG=model_vm_mdvr-kcl_knn_001
REMOTE_URI=${PREFIX}:${TAG}
docker build --platform linux/amd64 --provenance=false -t parked-vm-model .
docker tag parked-vm-model ${REMOTE_URI}
docker push ${REMOTE_URI}




